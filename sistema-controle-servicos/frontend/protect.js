// // Este script protege a página, garantindo que apenas usuários com token possam acessá-la.
// const authToken = localStorage.getItem('authToken');

// // Se o token NÃO existe, redireciona o usuário para a página de login.
// if (!authToken) {
//     // Aqui você pode decidir para qual login padrão redirecionar
//     window.location.href = 'login/login.html';
// }